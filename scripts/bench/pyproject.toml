[project]
name = "llm-serving-lab-bench"
version = "0.1.0"
description = "Benchmark and feature validation tools for LLM Serving Lab"
requires-python = ">=3.11"
dependencies = [
  "aiohttp>=3.9",
  "openlit>=1.42.0",
  "requests>=2.31",
  "transformers>=4.45",
]

[dependency-groups]
dev = []
